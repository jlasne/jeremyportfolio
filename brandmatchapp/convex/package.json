{
  "name": "brandmatch-backend",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "description": "brandmatch on Convex: the three gates, the quota journal, the client API and the internal ops API.",
  "scripts": {
    "dev": "convex dev",
    "deploy": "convex deploy"
  },
  "dependencies": {
    "convex": "^1.45.0"
  },
  "devDependencies": {
    "@types/node": "^22.20.3",
    "typescript": "^5.6.3"
  }
}
